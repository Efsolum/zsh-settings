# -*- mode: sh; sh-shell: zsh;-*-

local TARGET_PATH="${HOME}/Libraries"
local LIBRARY="Imported/"
local IMPORT_PATH="${HOME}/Downloads/Books/"

cd $TARGET_PATH
calibredb add --duplicates --library-path $LIBRARY $IMPORT_PATH
cd $HOME
